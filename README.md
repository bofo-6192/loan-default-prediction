# loan-default-prediction
A machine learning classification project that predicts whether a customer is likely to accept a personal loan offer. The model uses a Random Forest Classifier with hyperparameter tuning (GridSearchCV), feature engineering, scaling, and visualization. Dataset: Bank Personal Loan Modeling (CSV).
